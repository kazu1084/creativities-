<table class="table table-striped-columns mt-5">
    <tr>
      <td>名前</td>
      <td><%= @contact.name %></td>
    </tr>
    <tr>
      <td>email</td>
      <td><%= @contact.email %></td>
    </tr>
    <tr>
      <td>問い合わせ内容</td>
      <td><%= @contact.body %><br/>
      <%= link_to "delete", admin_contact_path(@contact), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger mt-3" %>
      </td>
    </tr>
  </table>